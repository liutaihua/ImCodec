####ImCodec
这是个按一个已有开源的算法的实现  
具体干的事情，就是把json结构的数据， 或者其他struct样式的数据，encode成拼接后的字符串。有一定的混淆作用, 在6个字段，10个汉字im消息时，大约节省25%的空间, 其他作用木有＝。＝  
因为拼接了下，略具混淆作用，好过json暴露。 field字段和嵌套多的情况下，比json节省空间更明显。   
参进其他自有协议里食用，混淆性更强。
